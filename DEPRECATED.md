# DEPRECATED

`zap-es` is superseded by **`@zap-proto/zap`** (the `zap-proto/ts` repo) — the
native ZAP TypeScript runtime: zero dependencies, its own wire format,
byte-compatible with `github.com/zap-proto/go` and the `luxfi/zap` transport.
New code MUST use `@zap-proto/zap`.
